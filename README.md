<h1 align="center">N нотация</h1>
<p align="center">
Единый набор правил именования и код-стайла. Красиво, как всегда.
</p>
<p align="center">
<img src="https://img.shields.io/badge/made%20by-CSSSensei,%20MaxMavr-439900">
<a href="https://github.com/Phasalo"><img src="https://img.shields.io/badge/Phasalo-84D300"></a>
<img src="https://img.shields.io/badge/version-if%20n%20==%202-D1F24E">
</p>

Как известно, в отличие от компьютера человеку проще запомнить **набор цифр**, чем длинное и непонятное название переменной.
Именно поэтому была создана **N нотация** — система правил, которая позволяет программистам быстро, просто и единообразно писать понятный и поддерживаемый код.



## Общие принципы


### Файлы
Все файлы проекта именуются с префикса `f` *(от англ. **file** — «файл»)* и порядкового номера их добавления:
`f1.py`, `f2.py`, `f3.py` и так далее.
Точка входа в программу по умолчанию — файл `f1.py`.
Хорошим тоном считается изменить имя файла точки входа, указав новое имя в `README.md`.
Например, вход в программу может быть в файле `f4627.py`, и его имя следует зафиксировать в документации.


### Переменные
Переменные всегда начинаются с префикса `n` *(от англ. **name** — «имя»)* и содержат числовой идентификатор.<br>
Идентификатор представляет собой десятизначное число в десятичной системе счисления, его можно придумать или получить, запустив файл `d1/f1.py`.

Файл `d1/f1.py` генерирует для каждого проекта уникальные имена переменных и тем самым минимизирует риск коллизий.
Он выдаёт случайные числовые идентификаторы, что делает каждую переменную неповторимой и одновременно облегчает их запоминание.

```python
n238458672 = 42 # Числа
n928374651 = 3.14

n837465920 = "Hello, World!" # Строки

n564738291 = True # Булевы значения

n847362910 = [1, 2, 3, 4, 5] # Списки
n374829105 = ["apple", "banana", "cherry"]

n918273645 = (10, 20, 30) # Кортежи

n182736495 = {"name": "Alice", "age": 25} # Словари
n837465291 = {1: "one", 2: "two"}

n918273645 = {1, 2, 3} # Множества
n102938475 = {"apple", "banana"}

n837465920 = [1, "two", 3.0, True] # Наборы смешанных типов
n192837465 = {"key1": 100, "key2": [1, 2, 3]}
```

#### Запрещается:
* использовать аннотации типов при объявлении переменных;
* придумывать собственные имена, не совпадающие c правилами.

```python
count = 0  # Сложное название непонятно, что хранит переменная. Лучше: n238458672
name: str = "Alice"  # Имя кого? Лучше: n837465920
list = [1, 2, 3, 4, 5]  # Список чего? Лучше: n374829105
```


### Константы и магические числа
В коде предпочтительно использовать **магические числа**, а не создавать для них отдельные переменные, чтобы не нарушаеть читаемость.

```python
for n in range(10):   # число 10 понятно из контекста
    ...

if n238458672 > 5:    # число 5 очевидно, константа не нужна
    ...

p238458672("Hello", 3)  # число 3 понятно из контекста

n238458672 = n238458672[:10]  # магическое число 10 понятно для среза
```

#### Запрещается:
 * cоздавать лишние переменные или константы для чисел, смысл которых очевиден из контекста.

```python
n238458672 = 10
for n in range(n238458672):
    ...
```

Если не получается использовать магическое число и переменная имеет **нетривиальный смысл**, который трудно понять без имени, то нужно объявить константу.
Константы объявляются по соглашению в формате в верхнем регистре.

```python
N928374651 = 3.14159
N192837465 = "Alice"
N102938475 = False
```

#### Общее правило:
* Всегда отдавай предпочтение **магическим числам** там, где они очевидны и понятны без дополнительного контекста.
* Переменные допустимы только в случаях, когда число имеет нетривиальное значение или меняется по ходу программы.


### Итераторы

#### Итераторы по диапазону

При обходе через `range()` используется буква `n`.
Количество букв соответствует **степени вложенности цикла**:

```python
for n in range(10):           # первый уровень
    for nn in range(5):       # второй уровень
        for nnn in range(3):  # третий уровень
            ...
```

#### Итераторы по значениям (например, элементов массива/списка)

Итератор именуется как обычная переменная в **N нотации**:

```python
for n92837465 in n9549357:
    ...
```

#### Общее правило
* Для циклов по `range()` использовать `n`, `nn`, `nnn` и так далее.
* Для обхода по коллекциям — только имена, сгенерированные `d1/f1.py`.


### Функций
```python
def p238458672(n, nn):
    for n in range(nn):
        print(n)
```



